package com.ctvit.vdp.framework.services;

import org.apache.commons.logging.Log;
import org.apache.commons.logging.LogFactory;

public class BaseServices {
	protected Log logger = LogFactory.getLog(getClass());
}
